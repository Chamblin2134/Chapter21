# Verification Notes

## Universal Intelligent Resource Categorization — local validation (2026-08-27)

The existing Store Resource Manager now analyzes selected PDF filenames and readable embedded text locally before any file is uploaded. It compares that information with normalized master-collection aliases and presents a confidence-ranked recommendation with editable title, collection, description, type, tags, page count, and suggested price fields.

A representative `100 Coping Strategies` PDF matched **Coping Skills** at 99% confidence, auto-assigned only because the optional high-confidence setting was enabled, and exposed all suggested fields for review. A weak **Values Reflection** match remained low confidence, listed its two closest existing collections, and required an explicit collection selection; **Choose Different Collection** returned focus to the standard editable selector. A no-match `Creative Recovery Collage` PDF correctly offered the editable proposed collection name **Creative Recovery Collage Resources** rather than silently selecting an unrelated collection. The non-destructive browser check did not call any Store write or upload operation.

Captured local browser evidence confirmed that the intake panel and staged metadata form are readable at 1280 × 720 and stack cleanly at exact 390 × 844 mobile. The product-card sizing, protected preview controls, resource files, Storage, authentication, cart, and existing published resources were not modified by this local recommendation validation.

## Store Detail Navigation and Resource Presentation — 2026-08-27 (content publication pending)

Confirmed that the reported Resources-tab prompt is the legacy `v9OpenClient` / `v9OpenClinician` overlay. It now includes **← Back to Collection**. The action closes the overlay, returns to the Store section, and restores focus to the active client or clinician collection filter. The current Store Library resource-details dialog retains its existing close options and now includes **← Back to Store Results**; it closes the dialog, returns to the Store Library area, and focuses the Store search field.

The Store Library now includes a concise **How previews work** note: cards use a watermarked first page; Preview browses public watermarked pages; and the original resource file stays private. The non-destructive browser check passed at desktop (1280 × 720) and exact mobile (390 × 844). It injected one temporary in-memory validation item only, verified both Back controls and their focus-return behavior, then restored the in-memory collection. No Store records, files, Storage configuration, schema, Edge Functions, cart, authentication, uploads, or preview protections were changed.

The current unsigned Store Library inspection showed an empty published-resource state. The latest permitted resource metadata inspection did not provide owner-approved published descriptions or public watermarked cover paths for the current Store products. To avoid inventing product content or images, the description-and-cover portion remains pending owner-saved metadata or publication of the existing records.

### Owner-supplied content verification — 2026-08-27

The owner subsequently saved a description for the published **Recovery Planning** resource. A limited read-only metadata check confirmed the saved description, published state, five-page count, and existing public page-one watermarked JPEG cover reference. No resource record, file, Storage policy, schema, or preview-generation behavior was changed during verification.

The live desktop Store card and details dialog show the owner’s description with the actual watermarked page-one cover. A separate unauthenticated exact 390 × 844 mobile test confirmed the description is readable in the details dialog, the card cover stays contained at 319 × 156 pixels, **← Back to Store Results** returns focus to Store search, and Preview opens exactly five protected JPEG pages while the preview iframe stays at `about:blank`. No browser errors occurred.

## Store Collection Card and Preview Sizing Refinement — 2026-08-27

The legacy Store cards that contain the collection title, count, carousel arrows, resource title, page/price details, Preview, and Add to Cart actions now use a fixed compact frame. On the desktop customer grid, visible empty and populated collection cards all measured **278 × 330 pixels**; at exactly 390 × 844, all visible cards measured **351 × 308 pixels**. No visible card had overflowing content.

The in-card PDF thumbnail height increased from 90 to **112 pixels** (approximately 24.4% larger). The thumbnail image now uses `object-fit: contain` with explicit maximum bounds, so the real watermarked Recovery Planning cover measured within its 162 × 112 desktop holder and 235 × 112 mobile holder without stretching. Empty placeholders occupy the full available carousel area when navigation arrows are hidden. The protected five-page JPEG Preview and customer cart addition/removal both passed at desktop and 390 px mobile.

## Dual-Audience Store Organization — 2026-08-27

The customer Store now leads with **Shop for Yourself**, **Shop for Clients**, and **Browse All Resources**, followed by keyword, Audience, Topic, and Resource type filters. The shared audience is stored and displayed as **Client + Clinician**; matching logic also understands the prior `Both` value so one resource is visible in both customer audiences without a copied record or file.

The Resource Manager keeps all owner-approved topics and formats available for future intake, including empty choices. By contrast, customer Topic and Resource type filter options are populated only from published resources, and legacy customer cards with no actual published resource are hidden automatically. The single published Recovery Planning record was normalized in place as **Client / Recovery Planning / Worksheet** with the Recovery Planning topic tag; no file, preview page, storage path, or second resource record was created.

Focused tests (13) passed. The non-destructive browser test passed at desktop and exact 390 × 844: Client browsing returned Client plus shared resources; Clinician browsing returned Clinician plus shared resources; Browse All returned each resource once; Topic, resource-type, and keyword filters each returned the correct item; absent Long-Term Recovery was hidden from customer topic options; and no horizontal overflow or browser error occurred. The existing unsigned mobile protected-preview and cart regression also passed.

## Unified Store Navigation — 2026-08-27

The public Store now has one primary audience navigation layer: **Shop for Yourself**, **Shop for Clients**, and **Browse All Resources**. The earlier visible **For Clients / For Clinicians** tab row, legacy audience notes, old filter rows, and duplicate static card grid are hidden from customers. Their useful category structure is retained through a dynamic category rail beneath the new audience choices: client resources display published recovery topics, clinician resources display published professional collections, and Browse All displays all published collections. The administrator Resource Manager retains the complete future client-topic and clinician-professional collection vocabulary.

The unified navigation check passed at desktop and exact 390 × 844 mobile. It confirmed legacy controls were not visible; client browsing returned client plus shared resources and client topics; clinician browsing returned clinician plus shared resources and professional collections; category buttons narrowed the same resource grid; Browse All returned each test resource once; and empty customer categories remained suppressed. The existing protected Preview and cart tests passed with no browser errors or horizontal overflow. No resource files, records, Storage settings, watermarks, preview generation, authentication, cart, checkout, or payment behavior was altered.

The live preview loaded the supplied Avery Institute document successfully. The header exposes the cart control and the source’s store and account controls are present. Opening the cart created a right-side drawer with a darkened backdrop, empty-cart state, subtotal, disabled secure checkout action, and close control. The desktop preview remained balanced after the drawer enhancement. The narrow viewport preview shows the compact brand mark, animated-menu trigger, cart icon, and preserved editorial hero composition. No browser console errors were emitted in the inspected logs.

The refreshed live preview exposes the cart toggle with the expected accessible label. Clicking it opens the drawer and makes the close control and secure checkout button visible; the checkout control is disabled for an empty cart. Store and account controls are exposed as buttons with tab roles, confirming the tab pass is attached to the live document.

Video-link restoration inspection found that the current source contains no YouTube, Vimeo, video, watch, or embed destinations beyond the PDF preview iframe. The published domain currently renders a blank root page with no console errors, while the local development preview renders the Avery page normally. This indicates the published deployment is stale or missing the current iframe-backed source asset; the link repair should also be validated against the next published checkpoint.

The current local preview successfully exposes Store navigation, client/clinician category tabs, resource filters, Resource Manager upload/drop-zone controls, bulk PDF upload, attach/replace PDF, remove-resource controls, manager filters, preview buttons, and Account navigation. The current source also contains the Account dashboard handlers and Supabase-backed upload, preview, category, attach, remove, and session code. The published domain previously showed a blank root despite serving avery-source.html; this should be treated as a deployment/configuration issue to recheck after the new checkpoint.

Final flow verification: Account navigation opens the Account panel with Sign In, Create Account, Forgot Password, Dashboard, My Downloads, Orders, Profile & Settings, and Password & Security tabs plus the sign-in form. Store navigation opens the current Store section with client/clinician category tabs, client resource filters, Resource Manager upload controls, preview buttons, and resource cards. No data was submitted or destructive action was performed.

Shared-file synchronization verification: the archive’s latest original.html was merged as the canonical source, preserving its category-specific multi-format upload controls, clickable YouTube statement links, preview download behavior, Account changes, and current resource-manager logic. The current cart quantity helpers and animated cart/mobile/tab layer were reapplied afterward. The merged source passed pnpm check and pnpm build; required feature markers are present in dist/public/avery-source.html. Desktop and mobile previews render successfully after the merge.

Production Supabase verification: the direct published source at /avery-source.html renders the full homepage, Store controls, and Account sign-in panel. The live Account section exposes Sign In, Create Account, password recovery, Dashboard, Downloads, Orders, Profile, and Password & Security tabs. The root app embeds this same document in an iframe; browser inspection confirmed the source frame is loaded and contains the complete rendered document.

Authenticated validation status: the live Avery production client initialized the Supabase client without error and, after administrator sign-in, reported an active authenticated session whose profile role is admin. The published Store section loads its client and clinician controls, category-level PDF upload prompts, and the administrator multi-format resource management interface.

Multi-format path inspection: the current customer-facing Store source intentionally exposes category-level PDF-only controls and no administrator multi-format manager fields in the rendered DOM. A controlled DOCX upload therefore uses the authenticated Supabase Storage bridge directly, matching the same private resource-files bucket and MIME-type behavior used by the multi-format manager code path.

Controlled authenticated multi-format validation: an authenticated administrator uploaded a disposable 1.5 KB DOCX to resource-files using the Word MIME type. Supabase returned the private object path successfully. The same administrator then created a 60-second signed URL and fetched the object with HTTP 200 and the expected Word MIME type. This confirms the authenticated private Storage upload and signed retrieval policies work for the multi-format DOCX path.

Validation cleanup and database-record preparation: the first disposable DOCX object was removed successfully under the same administrator policy. A new disposable DOCX object was then uploaded to validate that the current client persists MIME metadata with the associated resources row before both test artifacts are removed.

Full authenticated multi-format validation complete: the administrator created an unpublished resources row for the disposable DOCX and the returned record persisted the expected Word MIME type, private storage path, and administrator ownership. The test then removed both the temporary resources row and its private Storage object successfully, leaving no test product or file in production.

Final production status: the add_resource_mime_type_for_multiformat_uploads migration is present in Supabase migration history, and the client now writes mime_type on new file-backed resource records. The published production source renders the Store and Account surfaces, and the final pnpm check and pnpm build completed successfully. The production Site URL was set to https://averyinsti-qnbmu2v8.manus.space by the project owner. Remaining security advisory: Supabase reports that leaked-password protection is disabled; enable it in Auth settings before broadly inviting users.

Mobile header and hero repair: a mobile-only `@media (max-width: 600px)` override was added to the canonical Avery source. It uses a compact three-column header grid for the unmodified branding, hamburger, and cart; preserves the supplied hero photograph; uses the requested `clamp(42px, 11vw, 58px)` headline sizing; reduces hero spacing; makes body copy legible at 17–19px; stacks the existing CTAs at a safe full width; and removes the statement strip’s mobile horizontal scrolling. Desktop and tablet rules were not altered. The page was reviewed at 320px, 360px, 375px, 390px, 412px, 430px, and 1280px. `pnpm check` and `pnpm build` passed.

Version 4 Edge Function renderer diagnosis and repair: before modification, the deployed Version 4 function record was copied to `supabase/functions/generate-resource-preview/backups/deployed-version-4.before-rasterizer-repair.json`. Its ImageMagick WASM implementation returned `422 Unable to rasterize this PDF for preview` because PDF decoding needs an external Ghostscript/PostScript delegate that does not exist in the hosted WebAssembly build. A first PDFium browser/base64 test was rejected by the Edge runtime as not compiled for its environment; the server PDFium package entry point succeeded at rasterizing the private PDF, but ImageMagick still could not decode the emitted raw RGBA page buffer (`NoDecodeDelegateForThisImageFormat`). The repaired function keeps the same name, request body, JWT/admin gate, private `resource-files` input, and public `resource-thumbnails` output. It now uses the server-targeted bundled PDFium WebAssembly renderer for PDF pages and a pure-JavaScript pixel watermark/JPEG encoder, avoiding unsupported PDF and raw-image decoder delegates.

Authenticated real-PDF function validation: the repaired `generate-resource-preview` function returned HTTP 200 for `e4a5a2e9-7108-46c4-a10a-2a3ed91a1b42` and returned eleven explicit preview paths. The resource row now reports `page_count = 11` and page-one thumbnail path `e4a5a2e9-7108-46c4-a10a-2a3ed91a1b42-full-preview-page-1.jpg`; its original PDF path remains in the private `resource-files` bucket. Public preview pages 1 and 11 were inspected directly as 612 × 792 JPEGs and visibly contain repeated permanent `AVERY INSTITUTE`, `PREVIEW ONLY`, and `AVERYSINSTITUTE.COM` pixel watermarks. The remaining validation is to publish the Store handler update, refresh the Store as a customer, and confirm its modal uses the generated JPEG page set.

Published Store validation complete: after a fresh direct load of the published Avery source, the Store listed the workbook as an eleven-page `WATERMARKED PREVIEW` resource. Selecting Preview opened the customer-facing modal with the notice that it contains heavily watermarked preview pages rather than the original downloadable PDF. The modal rendered page 1 and page 2 from the `resource-thumbnails` JPEG set and retained its internal scroll for the remaining pages. The title, visible images, and Store controls confirmed that the refreshed frontend consumes the function response’s explicit `previewPaths`; no private PDF link was exposed in this customer modal.

All-page customer-preview verification: HTTP HEAD validation confirmed that preview pages 1 through 11 all exist as `image/jpeg` objects in `resource-thumbnails`. In the refreshed Store modal, the rendered preview set contained exactly eleven public thumbnail JPEG URLs. The only iframe element in the modal was hidden with `src="about:blank"`; no private-PDF link appeared. This confirms the Store cover is page 1 and the customer preview surface renders only the persisted, watermarked JPEG pages.

Administrator-only original-PDF fallback validation: using the existing authenticated administrator session and an unrelated private Store PDF with no generated thumbnail, a 300-second `resource-files` signed URL was issued successfully. A subsequent request to that temporary URL returned HTTP 200 with `application/pdf`. This matches the existing administrator-only fallback branch: customers receive only watermarked JPEG previews, while a clean original is never publicly linked and is available only through a short-lived authenticated administrator URL.

Second-document preview-quality validation: the repaired generator was run on the distinct private **Who Am I?** worksheet PDF, rather than the earlier workbook. It completed with HTTP 200 and generated six preview pages. Each public `resource-thumbnails` object returned HTTP 200 as `image/jpeg`, with file sizes from 54,719 to 63,606 bytes. The first and sixth pages were inspected at 612 × 792 pixels: worksheet headings, prompts, and form lines remain readable at preview scale, while three repeated in-image watermark bands and the footer watermark visibly include **AVERY INSTITUTE**, **PREVIEW ONLY**, and **AVERYSINSTITUTE.COM**. An unauthenticated request for the clean original returned a JSON error (HTTP 400), not PDF content.

About Me revision: the public About section now uses the **ABOUT ME** label and the **Therapeutic Approaches** heading. The previous Institute overview, framework introduction, Pattern/Purpose/Progress summary, biography, vision, and disclaimer are no longer rendered in the section. The ten existing therapy/framework cards remain unchanged. The revised section was inspected at desktop width and at a 390 px mobile viewport; the two-column desktop treatment and stacked, readable mobile cards remain contained without changing the homepage, Store, cart, authentication, or existing mobile rules.

Store carousel preview-return repair: the legacy carousel arrow listener rerendered resource cards from an older seed-data renderer, which discarded the watermarked-cover field after moving away from a generated resource. The current resource layer now intercepts arrow clicks during document capture, updates the existing carousel index, and rerenders from the current `resourceDb` entry before the legacy handler can overwrite it. On the published site, the Relationships carousel moved from the watermarked eleven-page workbook to the next resource and back; the returned workbook retained its watermarked cover. Its Preview control reopened a modal containing all eleven loaded 612 × 792 public JPEG pages. The customer modal still used no visible PDF iframe or original-PDF link.

Administrator fallback-control follow-up: the temporary original-PDF control was a styled `button`, so assigning its `href` and `download` properties did not create a clickable download destination. It is now converted at initialization to a same-styled anchor with `role="button"`. Published-session validation opened a private PDF with no thumbnail through the existing administrator-only fallback: the iframe displayed a signed `resource-files` URL, and the visible anchor had both a signed private href and download attribute. Returning to the customer workbook preview produced eleven public JPEG preview pages while the download anchor was hidden and had no href. No private original link is exposed in the customer branch.

Store Resource Manager access repair: the signed-in browser session and connected database profile both confirmed administrator status, but the manager was absent because legacy Store code inserted the `v14Admin` details element and immediately removed it. The one removal is now safely prevented while the existing `isAdmin()` visibility gate remains in force. On the published site, the signed-in administrator sees the **Store Resource Manager — Upload & Manage Resources** disclosure beneath the Store filters. Opening it exposes the single-file upload, bulk upload, attach/replace, resource removal, and management search controls. These controls remain restricted to administrators.

Customer visibility follow-up: after clearing the sandbox browser’s local session and reloading the published Store page, the Store Resource Manager was absent from the visible and interactive page controls. The Store continued to show customer resource collections, carousel navigation, Preview buttons, and cart actions, but no manager disclosure, upload form, replacement-file control, removal control, or resource-management filters.

Accurate Store counts: the displayed “10 resources” totals came from legacy seed cards created only as a visual placeholder while the Store loads; they were not uploaded PDFs or real Store records. The Store now removes those placeholders in every category once the published resource query returns. Live unsigned validation confirmed that **Relapse Prevention** displays its real count of three published resources and retains its preview control, while **Addiction and the Brain** displays “No published resources in this collection” with no Preview button or carousel arrows. The working preview system was not changed.

Explicit production evidence: on the published unsigned Store, Relapse Prevention reported **3 resources in this collection**, showed its Preview control, and retained two carousel arrows. Addiction and the Brain reported **No published resources in this collection**, displayed the empty-state message, and had neither a Preview control nor visible carousel arrows.

Trust & Safety policy integration: the existing `#privacy` route was expanded into a structured Trust & Safety page containing the owner-supplied Privacy Policy, Terms of Use and Terms of Purchase, Cancellation/Refund and Digital Purchase Policy, Coaching/Education/Clinical-Care Boundaries, and General Educational Disclaimer. The footer link now reads **Trust & Safety**. The policy sections use accessible disclosure controls, with Privacy open by default and the remaining longer sections available without leaving the page. Desktop and 390 px mobile checks confirmed readable typography and contained controls. Published-route verification confirmed all five policy disclosures and the updated footer link are live. Store, customer preview, administrator download fallback, cart, and authentication code were not changed.

About Me narrative restoration: the prior therapy-only rendering was intentionally replaced with the owner-provided narrative that belongs on the About Me page. It now includes the Institute’s purpose, whole-person recovery approach, Pattern/Purpose/Progress framework, Avery’s direct-work background, professional development and Ohio CDCA credential, applied-learning philosophy, recovery and human-development perspective, personal motivation, long-term vision, and the existing therapeutic approaches. Desktop and 390 px mobile layouts were inspected. Published validation confirmed the full narrative is available under `#about`, while Trust & Safety remains exclusively under `#privacy`; Store and preview code were not changed.

About Me photo placeholder: a professional-portrait placeholder was added beside the About Me introduction, using the existing charcoal and brass treatment. It is labeled **Avery Professional Portrait** and **Photo placeholder — replace with Avery’s photograph**, with an accessible description and a stable `avery-professional-photo-placeholder` target for a future replacement. It was visually checked at desktop and 390 px mobile sizes, then rechecked on the published `#about` route. The placeholder is visible, centered on mobile, and leaves Store, preview, and policy content unchanged.

About Me label consistency: the primary navigation and footer use **About Me** for the existing `#about` destination. The updated label was checked locally at desktop and 390 px mobile widths and on the published route; the destination, restored narrative, photo placeholder, Trust & Safety page, Store, and preview behavior remain unchanged.

Primary About Me label correction: the published desktop primary navigation now presents **About Me** as one complete visible label rather than splitting its wording. Browser inspection confirmed both the primary header and footer links have visible text **About Me** and retain `href="#about"`. The compact mobile layout was rechecked at 390 px; the About Me section destination and content remain intact.

Photo replacement handoff: when Avery’s real professional portrait is ready, provide the image in the project chat. The image will be retained outside the source tree, uploaded as a managed static asset, and inserted in place of the current artwork inside `#avery-professional-photo-placeholder`. The stable figure ID and surrounding responsive layout will remain; the replacement will use descriptive alternative text and cover the existing portrait frame without affecting the About Me narrative or other sections.

Site credibility roadmap (planning only; no related changes were made): priority one is clear practitioner identity and service boundaries—a real professional photo, biography, truthful qualifications, location/service area, and a plain statement of what coaching is and is not. Priority two is an unmistakable next step: a dedicated consultation/booking path, contact details, and expected response timing. Priority three is operational trust: a visible privacy notice, Terms of Service, cancellation/refund policy, and safety/crisis guidance. Priority four is brand ownership: a custom domain and matching professional email address. Priority five is a quieter public resource library with consistent archival-style covers; management controls should remain administrator-only. Any testimonials or reviews should be added only when they are genuine, consented to, and attributable.

Store Resource Manager upgrade: added a dedicated manager enhancement and an additive database migration for audience, subcategory, collection/series, volume, order, subtitle, author, and publisher metadata. Administrators can now stage multiple PDF, DOCX, and PPTX files locally before choosing **Save selected as Draft** or **Publish selected**; edit individual resource details; select/filter/sort resources; use non-destructive bulk actions; duplicate metadata; and deliberately delete/retire records. PDFs are sent only to the existing protected preview generator and a PDF that cannot generate a watermarked JPEG preview is retained as an unpublished private draft. The customer Store now has a searchable Store Library populated only from real published resources, with resource types, series and volume context, details, cart actions, and the existing protected Preview action. Legacy category upload controls are hidden from every public customer view.

Store Manager validation: the administrator session mounted the Secure Library Dashboard after sign-in, showed 14 saved resources, opened an individual metadata editor with 16 fields, completed a no-content-change resource save successfully, and exercised bulk selection (14 then cleared), local staging, and draft/public management controls. The session was explicitly signed out through the authenticated client afterward. Unsigned validation confirmed the manager and all legacy category upload controls are hidden; the public Store Library remained available with six real cards, and its Preview opened the existing protected modal with five watermarked JPEG pages. A direct unsigned request for a private original returned no data. Final JavaScript syntax validation, two focused Vitest files (four assertions), TypeScript check, and production build passed.

Controlled complete manager lifecycle validation: after a fresh administrator sign-in, a disposable DOCX was staged in the v15 manager, given a test title, category, price, and one-page count, then saved through the real **Save selected as Draft** control. The saved row was confirmed as unpublished with a private storage path. Its subtitle was edited and saved through the v15 metadata editor. The same draft was then deleted through the manager’s confirmed delete action; follow-up database and Storage reads confirmed both the disposable row and private file were gone. No test resource, preview, or public Store item remains.

Unsigned mobile Store validation: the Store was rendered at a 390 px mobile viewport with the new customer Store Library present and the administrator manager absent. In the signed-out customer context, the public Preview action opened the existing protected preview modal with five watermarked JPEG images, and Add to Cart opened the existing cart with the selected item and subtotal. The test cart item was removed, returning the cart to zero. The customer context continued to have zero visible legacy upload controls and no access to the private originals.

Explicit 390 px unsigned interaction test: a headless browser context was set to exactly 390 × 844 pixels and loaded the signed-out Store. The automated interaction confirmed six real customer Store Library cards, zero visible manager controls, zero visible legacy upload controls, a protected Preview modal with five JPEG preview images, successful cart add and removal, an empty cart after cleanup, and no page errors. This confirms the interactive mobile customer flow rather than layout alone.

Store asset cache correction: the published source and enhancement asset were compared directly after the first deployment. The files matched the local v15 source, but a browser with the older unversioned enhancement URL could retain a prior cached script. The canonical Store page now uses a versioned v15 script URL so existing visitors request the current manager/public-library code after this final publish. The regression test covers that versioned reference.

Final published Store verification: the live direct source page loaded the **Store Library** customer section with six real published cards and the versioned `store-manager-v15.js?v=20260827-store-manager-v15` asset. In the unsigned customer context, the Resource Manager remained hidden and zero legacy category upload controls were visible. The public search, type/audience/collection filters, View Details, protected Preview, and Add to Cart controls are available. No private-original delivery path was added.

Remaining limitations: none were identified for the requested Store upgrade. The following constraints are intentional and remain in place: clean source PDFs are private, customers receive only permanently watermarked JPEG previews, PDFs cannot be published through the manager unless protected preview generation succeeds, and Store management remains restricted to authenticated administrators.

Store card thumbnail containment: corrected the customer Store Library’s public card CSS only. Every card now has one fixed thumbnail viewport—168 px desktop and 156 px at the existing 600 px mobile breakpoint—with hidden overflow and `object-fit: contain` on preview images. Image dimensions are constrained to the viewport, so generated portrait, landscape, square, large, and small JPEG previews cannot alter card width or height. The former substantial-card span is neutralized in this library, keeping the desktop grid even and the mobile list stacked. Recovery Planning and Avery Institute Identity Workbook measured 358 × 347 cards with 356 × 168 contained desktop covers; at 390 px, their covers measured 319 × 156 and remained contained. The existing intentional Preview action still opened the protected full-page modal; no Store data, files, storage, preview generator, watermark, cart, upload, authentication, or manager logic changed.

Named protected-preview check: at an exact 390 px mobile viewport, the Recovery Planning card opened the existing protected preview modal with five watermarked JPEG pages, and the Avery Institute Identity Workbook card opened the same modal with one watermarked JPEG page. On the desktop Store grid, both named cards remained 358 × 347 pixels with 168 px high thumbnail viewports; the intentionally large full-preview view appeared only after the Preview action was selected.

Final published thumbnail-containment verification: the live Store source loaded the customer Store Library with the fixed card treatment. Recovery Planning and all available Avery Institute Identity Workbook cards measured 358 × 347 pixels, each with a 356 × 168 px cover viewport. Every loaded preview image was 356 × 168 px or smaller and contained within its cover; a resource still awaiting its protected preview used the same bounded placeholder viewport. No card expanded to page-scale dimensions.

Store worksheet prompt close control: the public Store resource-detail prompt now has a visible circular **×** control in its upper-right corner, with the accessible label “Close resource details.” It is separate from the existing text Close action and calls the same native dialog close behavior. The control was exercised in the actual Store prompt at desktop size using temporary browser-memory-only test data, then verified at exactly 390 px in an automated browser test. The test confirmed it was visible, correctly labelled, and dismissed the dialog with no page errors; no Store record, file, cart item, authentication state, or preview was changed. The Store enhancement loader was versioned as `20260827-prompt-close-v1` so returning visitors receive the control instead of a cached older script.

Final published worksheet prompt verification: after the versioned script reference propagated, the live source loaded `store-manager-v15.js?v=20260827-prompt-close-v1`. In the live Store’s actual resource-detail dialog, the upper-right **×** was visible with the expected accessible label and closed the dialog when selected. The live confirmation used only one temporary in-memory browser card because no published resource metadata or files were modified.

Signed-out upload-control safeguard: after final close-control verification surfaced historical upload fields in browser accessibility output, the Store enhancer’s existing visibility selector was strengthened to target the legacy category-upload element at its actual Store-grid location. Local computed-style validation with the v2 asset confirmed the uploaded-field container is `display: none`; the hidden fields do not appear in the rendered page. The versioned release will be rechecked live before handoff.

Final local signed-out privacy validation: with the `20260827-prompt-close-v3` asset, the local Store page showed the customer Store Library and ordinary category cards only. It had zero rendered legacy category-upload containers, zero rendered upload/status fields, and a hidden Resource Manager (`hidden=true`, `display:none`). The legacy nodes are now both CSS-hidden and explicitly marked hidden/`aria-hidden`, while the tested worksheet detail prompt and its × close control remain unchanged.

Final published signed-out Store verification: the live source loaded `store-manager-v15.js?v=20260827-prompt-close-v3`. In the customer context, the Store Library remains present while the Resource Manager is hidden (`hidden=true`) and not rendered. The page has zero rendered legacy category-upload containers and zero rendered legacy upload/status fields. This preserves the public worksheet-detail × close control and ensures that only authenticated administrators can encounter resource-management controls.

Combined final live v3 verification: with the live signed-out v3 asset, a temporary browser-memory-only worksheet was opened through the normal **View Details** action. Its visible upper-right × control, labelled “Close resource details,” was selected and the dialog closed immediately. Before and after this interaction, the public Store Library remained present while the Resource Manager and every legacy upload container/status field had zero rendered instances. No record, file, preview, cart, or authentication state was changed.

## Secure Intake Drag-and-Drop — local layout evidence (2026-08-27)

The administrator-only Secure Intake panel now includes a compact **Drag & Drop Resources Here** control in the top-right of its existing panel. The desktop browser capture shows it beside the existing Upload Resources instructions and actions; the exact 390 × 844 mobile capture shows it stacked above the established individual review queue. The queue remains visibly independent per file and presents the filename, PDF type, byte size, review status, locally detected one-page count, editable metadata controls, topic recommendation, and individual **× Remove** control. The non-destructive desktop/mobile browser harness also confirmed that one chosen PDF plus three dropped PDFs create four separate staged items with distinct local recommendations, then removing one leaves three and **Clear queue** removes all. It recorded no database or Storage writes and no browser errors.

## Secure Intake AI Titles and Progress Feedback — local validation (2026-08-27)

Readable staged-PDF text is now sent only from an authenticated administrator browser to a same-origin server endpoint for an optional AI title suggestion; the original file bytes, private Storage path, signed URLs, and preview pipeline are not sent to or used by that endpoint. The endpoint verifies the existing Supabase session and `profiles.role = admin`, applies a per-administrator request limit, restricts the document excerpt to 12,000 characters, requests a strict structured `{ title }` response, and returns a neutral editable filename-title fallback if title generation is unavailable.

The Secure Intake queue now provides a per-resource progress bar and clear status from local reading through metadata preparation and AI title generation to **Ready for review**. A suggested title is shown separately with an explicit **Use this title** action, so the owner remains in control and can keep or edit the existing filename-based title. Non-destructive browser validation at 1280 × 720 and exact 390 × 844 used one chosen PDF plus three dropped PDFs, observed the interim AI-title progress state, confirmed four independent title suggestions, applied one suggestion to its editable title field, removed one item, cleared the queue, and recorded zero Storage/database writes and zero browser errors. Focused local categorization, Store Manager source, server endpoint success/forbidden/fallback, public Store navigation, unsigned protected-preview/cart, TypeScript, and production-build checks passed.

## Homepage Prompt Resource Recommendations — local validation (2026-08-28)

The existing four homepage coaching prompts now retain their topic-selection, scheduling, Preview, and cart interfaces while matching selected subjects to the current Store vocabulary. This bridges the legacy prompt IDs to owner-approved published topics such as **Relapse Prevention**, **Relationships & Boundaries**, **Understanding Addiction**, and **Meaning, Values & Purpose**. Recommendations are limited to currently published PDF resources with an attached private file and are capped at three ranked matches; no placeholder product, file, or database record is created.

Non-destructive browser validation at 1280 × 720 and exact 390 × 844 clicked representative topics in all four prompts, confirmed a matching PDF appeared directly beneath the existing scheduling actions, retained the `#contact` scheduling destination, and added each chosen resource to the existing cart. The desktop and mobile open-panel captures confirm readable scheduling, Preview, and Add to Cart controls. The test recorded zero Supabase Storage/database writes and zero browser errors. Focused categorization/Store source tests (16), the signed-out protected-preview/cart regression, unified customer navigation regression, TypeScript check, and production build passed.

## About Me Photo — local validation (2026-08-28)

The user-provided original photo now replaces the former About Me placeholder through a managed static-asset URL; the image was not edited, cropped, or regenerated. Its meaningful alternative text identifies Avery with Jaylee, and the existing decorative frame and caption remain within the established About Me design. Desktop review confirms the full square photo is contained in the portrait panel alongside the lead text. Exact 390 × 844 mobile review confirms the image stacks below the lead copy, remains fully visible in a contained frame, and does not obstruct the About Me narrative.

## About Me Photo Caption Removal — local validation (2026-08-28)

The visible photo-caption panel, including the personal name text and secondary line, has been removed. The user-provided unedited photo remains in the same contained gold-framed position with a concise non-visible alternative description. Desktop and exact 390 × 844 mobile review confirm the caption-free image remains fully visible, proportionate, and unobstructed.

## Desktop-Layout Phone Presentation — local validation (2026-08-28)

At the owner’s request, the public site now retains the established 1280 px desktop composition on phone devices rather than applying its former phone-specific header, hero, grid, and column rearrangements. Both the outer application shell and embedded canonical page explicitly allow browser zoom. Actual mobile emulation verified the full public site at 320, 360, 375, 390, 412, and 430 px device widths: each had a 1280 px layout viewport; the desktop header, row navigation, hero, and hero-copy measurements matched the 1280 px desktop reference; and the former `max-width: 780px` mobile breakpoint did not apply. A 390 px public-shell capture confirms the entire desktop composition remains visible at page scale for visitor pinch-zoom.

## Recovery Topic Filter Presentation — local visual validation (2026-08-29)

Desktop review confirms that the customer Store now presents all owner-supplied Recovery Topics as one compact, non-nested control group, with **All Recovery Topics** first. The nearby Keyword Search, Audience, Recovery Topic, and Resource Type controls remain visually separate. The 390 × 844 Store capture confirms the existing small-screen Store entry, audience cards, and Store Library frame remain contained; the complete Recovery Topic control group remains reachable through the page’s established desktop-layout and visitor-zoom presentation.

## Recovery Topic and Keyword Filter Correction — local validation (2026-08-29)

The Store now uses the supplied 31-item **Recovery Topic** taxonomy consistently in the public Store and Resource Manager. Recovery Topic is an explicit multi-select assignment stored with each resource’s existing `topic_tags`; free-text keywords remain separate. Customer Keyword Search now reads only title, description, and assigned tags. Selecting a Recovery Topic filters only items explicitly assigned to it, with a narrow legacy-category fallback only when a resource has no explicit Recovery Topic yet. The former category-derived topic rail, which could imply unrelated nested categories, has been removed.

The Resource Manager now provides the same Recovery Topic checklist during staging and editing, retains separate Keywords / Tags, and includes an administrator-confirmed **Apply legacy topic mapping** action. The mapper converts only clear former topic labels to the new explicit taxonomy; it neither deletes resources nor modifies PDFs, preview files, prices, primary categories, cart, checkout, or authentication.

Focused source and taxonomy tests (17) passed. Deterministic desktop and 390 × 844 browser tests verified all 31 Recovery Topics are present, topic-only filtering, keyword-only filtering, combined filtering, legacy fallback, and the absence of old category buttons. Existing staged resource analysis, dual-audience discovery, homepage prompt-to-cart, signed-out protected-preview/cart, and desktop-layout-phone regressions also passed with no persistence writes or browser errors. Type checking and the production build completed successfully.

## Store Footer Link Placement — local validation (2026-08-30)

The visible Store top navigation now retains only Store-relevant links: Resources, Store, Account, Blog, Contact, and Cart. The duplicated Home, About Me, Services, and About Addictions links are hidden only while the Store is active and return normally on other site sections. Their destination links remain in the bottom footer’s Navigate group.

Initial page routing now waits until all page sections have been parsed and then places the shared footer after the final section. This corrects the original source order that positioned the footer before the Store section. Desktop visual inspection now shows the entire Store content first and the footer site map at the actual bottom. The deterministic footer-navigation regression passed at 1280 × 720 and 390 × 844: it confirmed hidden Store top links, visible Store/Account controls, visible footer links, successful footer navigation to About Me, and restoration of the regular top navigation outside Store view.

## Store Top Navigation Restoration — local validation (2026-08-30)

At the owner’s request, the standard top navigation is restored in the Store view. Home, About Me, Services, About Addictions, Resources, Store, Account, Blog, Contact, and Cart are once again visible at the top while the Store is active. The prior Store-specific hiding rule was removed; footer placement remains unchanged, so the full sitemap remains at the true bottom after Store content. Desktop visual inspection and the deterministic 1280 × 720 / 390 × 844 regression verified both the restored top links and working bottom-footer navigation. The focused source suite, TypeScript check, and production build passed.
