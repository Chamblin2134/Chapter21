# Homepage Prompt Resource Recommendations — Visual Review

The initial desktop full-page capture confirms the unchanged homepage service cards and Store controls remain present after adding the topic matcher. The browser interaction test verified the selected prompt topics render their matching in-memory PDF cards beneath the scheduling row, retain the scheduling link, and open the existing cart after selection without making any persistence write.

The initial full-page mobile capture preserved the page’s historical mobile canvas treatment but did not show the fixed open prompt panel needed to assess the new topic-resource controls. The focused browser harness has therefore been adjusted to capture the open prompt at the actual 390 × 844 viewport before its local cart interaction. That replacement capture will be used for the final responsive assessment.

The focused desktop capture shows the existing **Schedule an Appointment** and **Contact Avery Institute** actions followed immediately by the new Related Store PDFs panel. The visible one-card recommendation provides both protected **Preview** and explicit **Add to Cart** actions without crowding the modal. The corresponding exact 390 × 844 capture keeps the schedule action, count, explanatory copy, thumbnail placeholder, Preview, and Add to Cart controls readable and contained in the stacked prompt panel.
